# 样例,不要直接 apply。平台用:
#   kubectl -n gaussdb-agent create secret generic model-api --from-literal=MODEL_API_KEY='<密钥>'
apiVersion: v1
kind: Secret
metadata:
  name: model-api
  namespace: gaussdb-agent
type: Opaque
stringData:
  MODEL_API_KEY: "<模型服务密钥,由平台填写>"
